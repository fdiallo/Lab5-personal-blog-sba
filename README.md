This project allow us to manipulate the DOM, handle users events, implement form validation, and utilize localstorage for data persistance focusing mainly on client-side javascript. 

Tu run the applicaton, launch a local server which will open up the index.html in your preferred browser.

I followed the steps ourline in the project description to implement the different functionalities. 

I encounterred a few challenges while implementimg the functionalities. One of those challenge include finding the index of the post (the post object in the array of post objects) to be deleted and updated. I had to define a variable to track the current editing post index. 

Another challenges involved making sure the array posts and the array posts retrieve from the local are in sync. 



